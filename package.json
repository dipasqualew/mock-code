{
  "name": "mock-code",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile src/index.ts --outfile mock-code"
  },
  "devDependencies": {
    "bun-types": "^1.3.8"
  }
}
